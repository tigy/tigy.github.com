/** * @author xuld */imports("Controls.Display.Icon");imports("Controls.Button.CloseButton");imports("Controls.Display.Mask");imports("Controls.Container.Dialog");using("Controls.Core.ContainerControl");var Dialog =  ContainerControl.extend({		xtype: 'dialog',		resizable: true,		draggable: true,		autoCenterType: 1 | 2,		createHeader: function(){		var header = Dom.create('div', 'x-' + this.xtype + '-header');		header.setHtml('<a class="x-closebutton">×</a><h3></h3>');		header.find('.x-closebutton').on('click', this.close, this);		return header;	},		onClosing: function () {		return this.trigger('closing');	},		onClose: function () {
		this.trigger('close');
	},		init: function(options){				var t ;				// 允许直接传入一个节点。		if(!this.hasClass('x-dialog')){						// 判断节点是否已渲染过。			//var p = this.parent();			//if(p && p.hasClass('x-facedialog-body'))						// 保存当前节点。			t = this.node;			this.node = this.create(options);		}		this.setStyle('display', 'none');				// 移除 script 脚本。		this.query('script').remove();				if(t){			this.container().appendChild(t);		}	},		mask: function(opacity){		var mask = this.maskDom || (this.maskDom = document.find('.x-mask-dialog') || Dom.create('div', 'x-mask x-mask-dialog').appendTo());		if (opacity === null) {
			mask.hide();
		} else {			mask.show();			mask.setSize(document.getScrollSize());			if (opacity != null)				mask.setStyle('opacity', opacity);		}		return this;	},		setOffset: function(p){		if(p.x != null) {			this.autoCenterType &= ~2;			this.setStyle('margin-left', 0);		}				if(p.y != null) {			this.autoCenterType &= ~1;			this.setStyle('margin-top', 0);		}				return this.base('setOffset');	},		setWidth: function(){		return this.base('setWidth').center();	},		setHeight: function(){		return this.base('setHeight').center();	},		setHtml: function(){		return this.base('setHtml').center();	},		setText: function(){		return this.base('setText').center();	},		/**	 * 刷新当前对话框的位置以确保居中。	 */	center: function(){		if(this.autoCenterType & 1)			this.setStyle('margin-top', - this.getHeight() / 2 + document.getScroll().y);					if(this.autoCenterType & 2)			this.setStyle('margin-left', - this.getWidth() / 2 + document.getScroll().x);					return this;	},	show: function(){				if(!this.parent('body')){			this.appendTo();			}				return this.base('show').center();	},		showDialog: function(callback){		return this.mask().show(this.fadeInDuration, callback);	},		hide: function(){		this.base('hide');		if (this.maskDom) this.maskDom.hide();		return this;	},		dispose: function(){		var me = this;		this.onClose();		if (this.maskDom) this.maskDom.remove();	},		setContentSize: function(x, y){		this.setWidth('auto');		this.container.setWidth(x).setHeight(y);		this.center();		return this;	},		fadeInDuration: 300,		close: function(){		if(this.onClosing() !== false)			this.hide(this.fadeInDuration, this.onClose.bind(this));	},		removeCloseButton: function(){		this.find('.x-closebutton').remove();		return this;	}	});