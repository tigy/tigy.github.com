/** * @author  */imports("Controls.Display.Icon");using("Controls.Core.IMenuContainer");using("Controls.Form.CombinedTextBox");using("Controls.Form.MonthCalender");var DatePicker = Picker.implement({		menuWidth: -1,		menuButtonTpl: '<button class="x-button"><span class="x-icon x-icon-calendar"></span></button>',		init: function (options) {		this.base('init');		this.setMenu(new MonthCalender().on('itemclick', this.onItemClick, this));		this.button().on('click', this.toggleMenu, this);		this.on('focus', this.showMenu);	}, 		format: 'yyyy/M/d',		onSelect: function (value) {		this.setValue(value);				this.hideMenu();	},		// updateMenu: function(){
		// var d = new Date(this.getText());
		// if(!isNaN(d.getYear()))
			// this.menu.setValue(d);
	// },		getValue: function(){		return this.menu.getValue();	},		setValue: function(value){		this.setText(value.toString(this.format));		this.menu.setValue(value);		return this;	}});