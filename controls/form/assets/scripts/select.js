/** * @author  xuld */imports("Controls.Form.Select");using("Controls.Core.IInput");var Select = Control.extend(IInput).implement({		xtype: 'textbox',		tpl: '<select></select>',		add: function(name, value){		if(typeof name === 'string'){			name = new Option(name, value);			}		this.node.add(name);	},		remove: function(name, value){		if(typeof name === 'string'){			Object.each(this.node.options, function(option){				if(option.text === name || option.value === value) {					this.node.remove(option);					return false;				}			}, this);		} else {			this.node.remove(name);		}				return true;	}	});